[package]
name = "neo4j_schema"
version = "0.1.0"
edition = "2024"

[dependencies]
ureq = { version = "3", features = ["native-tls", "json"] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
base64 = "0.22"
